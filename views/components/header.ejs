<link rel="stylesheet" href="/static/css/style.css">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">ReservaSalas</a>
    <div>
      <ul class="navbar-nav">
        <li class="nav-item">
          <button id="toggleTheme" class="btn btn-sm btn-light ms-3">🌙</button>
        </li>
        <li class="nav-item"><a class="nav-link" href="/salas">Salas</a></li>
        <li class="nav-item"><a class="nav-link" href="/minhasReservas">Reservas</a></li>
        <li class="nav-item"><a class="nav-link" href="/reservarUsuario">Nova Reserva</a></li>
      </ul>
    </div>
  </div>
</nav>

<script>
  const toggleButton = document.getElementById("toggleTheme");

  function setTheme(dark) {
    document.body.classList.toggle("dark-mode", dark);
    localStorage.setItem("dark-mode", dark);
    toggleButton.textContent = dark ? "☀️" : "🌙";
  }

  const isDark = localStorage.getItem("dark-mode") === "true";
  setTheme(isDark);

  toggleButton.addEventListener("click", () => {
    setTheme(!document.body.classList.contains("dark-mode"));
  });
</script>